import numpy as np

a=np.array([(1,3,5,7,9,11)])

print(a.size) # find the size of the array (returns 6 )

print(a.shape)  # find the shape of the array  returns (1,6) that means it contains 1 rows and  6 columns