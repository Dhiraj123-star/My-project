import numpy as np

a=np.array([10,90,67])

print(a.min()) # finding the minimum value

print(a.max()) # finding the maximum value

print(a.sum()) # returns the sum of all elements of the array
