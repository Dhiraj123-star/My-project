#program to generate prime number using while loop
import math
num=int(input("enter your limit:"))
i=1

while i<=num:

  k=int(math.sqrt(i))
  j=2
  while j<=k:
        if i%j==0:break
        j+=1
  else:
           print(i)
  i+=1
