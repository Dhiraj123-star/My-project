#rectangle module -the rectangle module has following that performs calculations related to the rectangle
#the area function accepts a rectaangle's width and length as arguments and returns the rectangle's area
def area(width,length):
    return width*length

def perimeter(width,length):
    return 2*(width+length)
